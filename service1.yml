apiVersion: v1
kind: Service
metadata:
  name: my-service1
spec:
  type: NodePort
  selector:
    app: web-app1
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30008
