apiVersion: v1
kind: Service
metadata:
  name: my-service1
spec:
  type: nodePort
  selector:
    app: web-app1
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 30008
